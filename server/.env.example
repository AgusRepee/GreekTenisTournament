# MySQL — Prisma lee DATABASE_URL; alternativamente desde piezas (server/src/envBootstrap.ts)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=app_user
DB_PASSWORD=cambiar
DB_NAME=greek_tennis

DATABASE_URL="mysql://app_user:cambiar@127.0.0.1:3306/greek_tennis"

# API
PORT=3001
CORS_ORIGIN=http://localhost:5173,https://tu-dominio.com

# Admin — login (MVP) + JWT
ADMIN_PASSWORD=cambiar_seguro
JWT_SECRET=cambiar_usar_cadena_larga_aleatoria
JWT_EXPIRES_SECONDS=28800
